{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "sarif-lead.schema.json",
  "title": "OSWE SARIF Lead",
  "type": "object",
  "additionalProperties": false,
  "required": ["lead_id", "tool", "rule_id", "vuln_class_hint", "location", "message"],
  "properties": {
    "lead_id": { "type": "string", "pattern": "^L[0-9]{3,}$" },
    "tool": { "type": "string", "minLength": 1, "maxLength": 64 },
    "rule_id": { "type": "string", "minLength": 1, "maxLength": 256 },
    "vuln_class_hint": { "type": "string", "minLength": 1, "maxLength": 64 },
    "location": { "$ref": "#/$defs/loc" },
    "codeflow": { "type": "array", "maxItems": 64, "items": { "$ref": "#/$defs/loc" } },
    "message": { "$comment": "May be empty: ingestion sets it to '' when a SARIF result has no message.text; an absent message must not fail validation.", "type": "string", "maxLength": 512 }
  },
  "$defs": {
    "loc": {
      "$comment": "SARIF physical location: file + 1-based startLine only. Intentionally NOT finding.schema.json#/$defs/loc (which also requires symbol/kind and allows line 0) — a SARIF location carries neither, and SARIF startLine is 1-based, so line>=1.",
      "type": "object", "additionalProperties": false,
      "required": ["file", "line"],
      "properties": {
        "file": { "type": "string", "minLength": 1, "maxLength": 1024 },
        "line": { "type": "integer", "minimum": 1 }
      }
    }
  }
}
