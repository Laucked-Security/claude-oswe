node_modules/
.oswe/
# Third-party repos cloned locally for audit demos/testing — never commit (not ours).
external/
# Python bytecode from static-only fixture verification (py_compile) — never commit.
__pycache__/
*.pyc
